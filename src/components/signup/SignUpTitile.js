import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const SignUpTitile = ({title}) => {
  return (
    <Text style={{fontSize: 28}}>{title}</Text>
  )
}

export default SignUpTitile

const styles = StyleSheet.create({})